configs:
  infra:
    patternUrls:
      invitationUrlPattern: ${A6_MESSAGING_INFRA_PATTERNURLS_INVITATION:http://localhost:10700/clubs/:clubId/invitations/:invitationToken/accept}
      projectUrlPattern: ${A6_MESSAGING_INFRA_PATTERNURLS_PROJECT:http://localhost:10700/managements/:projectManagementId}
    patternPlaceholders:
      clubId: :clubId
      invitationToken: :invitationToken
      projectId: :projectId
      projectManagementId: :projectManagementId
    mailingConfigs:
      fromName: ${A6_MESSAGING_INFRA_MAILINGCONFIGS_FROMNAME:The AngoraSix Team}
      fromEmail: ${A6_MESSAGING_MAIL_USERNAME:myusername@gmail.com}
